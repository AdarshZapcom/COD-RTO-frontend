# Base URL of the FastAPI backend (task 05). Points at the local uvicorn
# instance during rehearsal; swap for the EC2 URL on demo day if task 07
# ships (otherwise leave as localhost).
VITE_API_BASE_URL=http://localhost:8000
